// const express = require("express");
// const router = express.Router();
// const bannerController = require("../controllers/bannerController");
// const { authenticateJWT, isAdminOrStaff } = require("../middleware/auth");

// router.get("/active", bannerController.getActiveBanners);

// router.get("/", authenticateJWT, isAdminOrStaff, bannerController.getAllBanners);
// router.post("/", authenticateJWT, isAdminOrStaff, bannerController.createBanner);
// router.put("/:id", authenticateJWT, isAdminOrStaff, bannerController.updateBanner);
// router.delete("/:id", authenticateJWT, isAdminOrStaff, bannerController.deleteBanner);

// module.exports = router;
